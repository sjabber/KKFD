/*
package com.kkfd;

import static org.junit.jupiter.api.Assertions.assertNotNull;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.context.ApplicationContext;

@SpringBootTest
class KkfdApplicationTests {

	@Autowired
	ApplicationContext context;

	@Test
	void contextLoads() {
		assertNotNull(context.getBean("checkloginedController"));
		assertNotNull(context.getBean("customerController"));
		assertNotNull(context.getBean("downloadController"));
		assertNotNull(context.getBean("repBoardController"));
	}

}
*/
